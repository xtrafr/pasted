<svelte:head><title>Import | Pasted</title></svelte:head>

<main class="route-start">
	<p>Import</p>
	<h1>Turn a messy file into a useful library.</h1>
</main>

<style>
	.route-start {
		display: grid;
		max-width: var(--page-max-width);
		gap: var(--space-5);
		margin: 0 auto;
		padding: clamp(3rem, 8vw, 8rem) var(--page-gutter);
	}
	.route-start p,
	.route-start h1 {
		margin: 0;
	}
	.route-start p {
		font-size: var(--text-caption);
		letter-spacing: var(--tracking-caption);
		text-transform: uppercase;
	}
	.route-start h1 {
		max-width: 13ch;
		font-family: var(--font-display);
		font-size: var(--text-heading);
		font-weight: var(--font-weight-regular);
		letter-spacing: -0.04em;
		line-height: 0.92;
	}
</style>
